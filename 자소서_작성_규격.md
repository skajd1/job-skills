# 자소서 작성 규격

## 기본 원칙

- 모든 자소서는 Markdown(`.md`)으로 작성합니다.
- 작업 흐름은 항상 [자소서_작업_워크플로우.md](자소서_작업_워크플로우.md)를 따릅니다.
- 문항이 2개 이상이면 문항별 초안 작성은 subagent에 분담하고, 최종 통합과 검수는 메인 Codex가 수행합니다.
- 실제 기업 자기소개서는 `result/기업명/` 폴더 아래에 직무별 파일로 둡니다.
- 파일 상단에는 지원 정보를 메타데이터로 정리합니다.
- 문항은 `## 문항 N. 문항 내용` 형식으로 통일합니다.
- 글자 수 제한은 문항 바로 아래 `- 제한: 000자 이내` 형식으로 적습니다.
- 답변 제목은 `### 답변 제목` 아래에 대괄호 제목으로 적습니다.
- 실제 답변은 `### 답변` 아래에 작성합니다.

## 파일명 규칙

권장 형식은 다음과 같습니다.

```text
result/기업명/직무명YYMM.md
result/기업명/직무명-YYMM.md
result/기업명/직무명(팀명)YYMM.md
```

예시:

```text
result/삼성SDS/소프트웨어2603.md
result/롯데이노베이트/AI기술팀2603.md
result/KINX/시스템운영엔지니어-2604.md
```

## 파일 내부 형식

모든 자소서는 아래 순서를 따릅니다.

```markdown
# 기업명 - 직무명 (YYMM)

- 기업: 기업명
- 직무: 직무명
- 지원시기: YYMM
- 상태: 초안

## 문항 1. 문항 내용

- 제한: 000자 이내

### 답변 제목

[답변을 대표하는 한 줄 제목]

### 답변

답변 본문
```

## 답변 작성 규칙

- 문항 원문은 가능한 한 채용 사이트 문구 그대로 보존합니다.
- 답변 제목은 핵심 역량, 경험, 메시지가 드러나게 작성합니다.
- 작성 전 [자소서_작성_요령.md](자소서_작성_요령.md)에 따라 JD의 직무 미션, 요구 역량, 기술 키워드, 평가 포인트를 먼저 정리합니다.
- 각 답변은 기업의 필요와 직무 문제에 먼저 답하고, 그 근거로 본인 경험을 연결합니다.
- 이력서나 경험 목록을 반복하지 말고 문항별로 가장 강한 경험 1~2개만 선택합니다.
- 한 문항 안에서는 `지원 동기 -> 근거 경험 -> 입사 후 기여` 흐름을 우선합니다.
- 경험 문항은 `상황 -> 행동 -> 결과 -> 배운 점/직무 연결` 흐름을 권장합니다.
- 경험 문항은 가능하면 `상황 -> 과제 -> 행동 -> 결과 -> 직무 연결`의 STAR 구조로 씁니다.
- 같은 소재를 여러 기업에 재사용하더라도 마지막 문단은 반드시 기업명과 직무에 맞춰 조정합니다.
- 수치 성과가 있으면 답변 본문에 직접 포함합니다.
- 수치가 없을 때는 기술명, 시스템 범위, 담당 역할, 산출물, 협업 대상을 구체 명사로 적습니다.
- `책임감`, `열정`, `소통 능력`, `문제 해결력` 같은 추상어는 사례 없이 단독 주장으로 쓰지 않습니다.
- 문단은 3~5문장 안에서 끊고, 첫 문장에는 문항에 대한 핵심 답을 둡니다.

## Humanizer 검수 규칙

- 최초 초안 작성 후 [humanizer_사용법.md](humanizer_사용법.md)에 따라 `$humanize-korean` 스킬로 문체를 반드시 점검합니다.
- 윤문 대상은 가능하면 파일 전체보다 문항별 `### 답변` 본문으로 나눕니다.
- 윤문 결과는 별도 파일로 분리하지 않고 같은 `.md` 파일의 답변 본문에 덮어씁니다.
- 최종 산출물은 `result/기업명/` 폴더 안의 Markdown 파일 1개만 유지합니다.
- 기업명, 직무명, 프로젝트명, 기술명, 수치, 날짜는 원문과 동일하게 보존합니다.
- `~를 통해`, `~에 대해`, `결론적으로`, `이를 통해`, `따라서`, `~할 수 있다`가 반복되면 문맥에 맞게 줄입니다.
- 문체는 자연스럽게 다듬되 자기소개서의 격식체와 지원자의 경험 구조는 유지합니다.
- 윤문 후에는 반드시 글자 수 제한과 기업 맞춤 키워드가 유지됐는지 다시 확인합니다.

## 검토 체크리스트

- JD의 요구 역량과 답변의 경험 근거가 직접 연결되어 있는가
- 회사명만 바꿔도 통하는 일반론 문장이 남아 있지 않은가
- 문항별로 가장 강한 경험 1~2개에 집중했는가
- 주장마다 경험, 수치, 기술명, 산출물 중 하나 이상의 근거가 있는가
- 기업명, 직무명, 지원시기가 파일 상단 메타데이터와 파일명에서 일치하는가
- 모든 문항에 제한 글자 수가 적혀 있는가
- 문항별 답변 제목과 답변 본문이 분리되어 있는가
- 다른 기업명이 본문에 남아 있지 않은가
- 반복 소재라도 해당 기업의 사업, 인프라, 직무 키워드와 연결되어 있는가
- 맞춤법, 띄어쓰기, 문장 종결 표현이 통일되어 있는가
- Humanizer 윤문 후에도 의미, 수치, 고유명사, 직무 키워드가 보존되어 있는가

## 상태 값

`상태` 항목은 아래 중 하나를 사용합니다.

- 초안: 최초 작성 또는 큰 수정 전
- 검토중: 분량, 표현, 기업 맞춤성을 다듬는 단계
- 제출본: 실제 제출에 사용한 최종본
- 보류: 지원 취소 또는 재작성 예정
